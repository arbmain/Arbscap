# Netlify configuration for Next.js 16 with custom webpack

[build]
  command = "npm run build --webpack"  # Build command
  publish = ".next"                     # Output directory for Next.js

[dev]
  framework = "next"
  port = 8888
